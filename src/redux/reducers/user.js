const USER_INITIAL_STATE = {
  userTeste: 'teste',
};

const userReducer = (state = USER_INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default userReducer;
